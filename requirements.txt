# VoiceDraft — dependencias
# Instala con: pip install -r requirements.txt

# GUI
PyQt6>=6.6.0

# Audio
sounddevice>=0.4.6
numpy==1.26.4

# Hotkey global (Windows + Linux)
keyboard>=0.13.5

# Transcripción local (Whisper con CUDA)
faster-whisper>=1.0.0

# HTTP (APIs)
httpx>=0.27.0

# Portapapeles
pyperclip>=1.8.2

# Desarrollo — auto-restart al guardar cambios (no necesario en producción)
watchdog>=4.0.0

# Compilación (Empaquetado)
pyinstaller>=6.0.0
